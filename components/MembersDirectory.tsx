"use client";import{FormEvent,useEffect,useState}from"react";
type P={user_id:string;username:string;display_name:string;bio?:string;role:string;genres?:string;location?:string;looking_for?:string;profile_image_url?:string|null};
export default function MembersDirectory(){const[ps,setPs]=useState<P[]>([]);const[loading,setLoading]=useState(true);async function load(q=""){setLoading(true);const r=await fetch(`/api/members${q?`?q=${encodeURIComponent(q)}`:""}`);const x=await r.json()as{profiles?:P[]};setPs(x.profiles||[]);setLoading(false)}useEffect(()=>{void load()},[]);function search(e:FormEvent<HTMLFormElement>){e.preventDefault();void load(String(new FormData(e.currentTarget).get("q")||""))}return <><form onSubmit={search} className="mt-10 flex max-w-2xl gap-3"><input name="q" placeholder="Search singers, genres, or stage names" className="min-w-0 flex-1 rounded-full border border-white/15 bg-white/[.04] px-5 text-white outline-none focus:border-[#f4b400]"/><button className="primary-cta">Search</button></form>{loading?<p className="mt-12 text-white/50">Opening the StageFront community...</p>:<div className="mt-12 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{ps.map(p=><a key={p.user_id} href={`/singers/${p.username}`} className="overflow-hidden rounded-3xl border border-white/10 bg-white/[.035] transition hover:-translate-y-1 hover:border-[#f4b400]/50"><div className="aspect-square bg-[#111118] bg-cover bg-center" style={p.profile_image_url?{backgroundImage:`url(${p.profile_image_url})`}:{}}>{!p.profile_image_url?<div className="grid h-full place-items-center text-7xl font-black text-[#f4b400]/35">{p.display_name[0]}</div>:null}</div><div className="p-6"><p className="text-xs font-black uppercase tracking-[.2em] text-[#f4b400]">{p.role}</p><h2 className="mt-2 font-display text-2xl font-black">{p.display_name}</h2><p className="text-white/45">@{p.username}</p>{p.genres?<p className="mt-4 text-sm text-white/65">{p.genres}</p>:null}{p.bio?<p className="mt-3 line-clamp-3 text-sm leading-6 text-white/50">{p.bio}</p>:null}</div></a>)}</div>}</>}
